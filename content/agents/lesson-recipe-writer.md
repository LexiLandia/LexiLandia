# LexiForge Agent: Lesson Recipe Writer

Goal: create compact `*.lesson.json` recipes.

Use:
- `content/dictionary/ru.json`
- one requested lesson idea

Output:
- one complete lesson source JSON object.

Prefer recipes:
- `vocabulary`
- `map`

Keep `status` as `draft` unless the user says ready.
