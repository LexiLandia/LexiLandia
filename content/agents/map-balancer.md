# LexiForge Agent: Map Balancer

Goal: check if map commands are clear and fair.

Checks:
- target is visible;
- start and target are inside grid;
- distractors do not confuse the learner too much;
- map fits phone screen;
- command can be solved by listening, looking, and moving.

Output:
- concise findings and suggested JSON edits.
